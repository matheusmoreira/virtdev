# user.conf - local build settings
#
# Committed with safe defaults.
#
# To hide changes after customizing:
#
#     git update-index --skip-worktree user.conf
#

TIMEZONE=America/Sao_Paulo
SSH_KEY_PATH="${HOME}/.ssh/virtdev_id"
